<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="gallery_board">
	<!-- 글쓰기 -->
	<insert id="gBoardWrite">
		insert into tbl_gallery_board(g_no, g_title, g_location, g_content, g_writer, g_file_path)
		values(seq_g_board.nextval, #{g_title}, #{g_location}, #{g_content}, #{g_writer}, #{g_file_path})
	</insert>

	<!-- 글조회 -->
	<select id="gBoardRead" resultType="GBoardVo">
		select *
		from tbl_gallery_board
		where g_no = #{g_no}
	</select>
	
	<!-- 글수정 -->
	<update id="gBoardUpdate">
		update tbl_gallery_board set
			g_title = #{g_title},
			g_location = #{g_location},
			g_content = #{g_content},
			g_file_path = #{g_file_path}
		where g_no = #{g_no}	
	</update>
	
	<!-- 글삭제 -->
	<delete id="gBoardDelete">
		delete from tbl_gallery_board
		where g_no = #{g_no}
	</delete>
	
	<!-- 글목록 -->
	<select id="gBoardList" resultType="GBoardVo">
		select *
		from tbl_gallery_board
		order by g_no desc
	</select>
	
	<!-- 조회수 증가 -->
	<update id="gViewCount">
		update tbl_gallery_board set
			g_viewcnt = g_viewcnt + 1
		where g_no = #{g_no}	
	</update>
	
	
</mapper>  